# core/settings/__init__.py

import os

if os.environ.get('DJANGO_ENV') == 'production':
    from .production import *
else:
    # Default to development if DJANGO_ENV is not set or not 'production'
    from .development import *